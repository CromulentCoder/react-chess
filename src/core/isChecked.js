// Take last move & new snapshot:
export default function kingIsChecked() {
    // Check if there is a check
    // Return true if there is a double check
        // {In main program: If double check get possible king moves}

    // Find the squares from where king is checked and return the squares which need to be protected
}